<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allegro.encryptor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnigmaIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DisplayEnigmaCipheredMessage"
            android:label="@string/title_activity_display_enigma_ciphered_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OneTimePadIn" />
        <activity android:name=".DisplayOTPCipheredMessage" />
        <activity android:name=".Rot13In" />
        <activity android:name=".DisplayRot13CipheredMessage"></activity>
    </application>

</manifest>